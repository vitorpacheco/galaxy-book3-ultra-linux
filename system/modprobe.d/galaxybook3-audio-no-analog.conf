# Galaxy Book3 Ultra: the ALC298 analog codec (address 0) is faulty and hangs
# every process that opens its PCMs. Probe only the HDMI codec (address 2);
# SOF then uses the HDMI + DMIC topology.
options snd_sof_intel_hda codec_mask=4
